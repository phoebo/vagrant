upstream singularity {
  server 127.0.0.1:7099;
}

server {
    listen 80;
    server_name singularity.<%= @fqdn %>;
    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_pass http://singularity/singularity/;
    }
}