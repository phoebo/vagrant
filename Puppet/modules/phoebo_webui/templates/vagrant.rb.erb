# Vagrant specific configuration
# We do this, because we don't run actual DNS in our Vagrant environment

SingularityConnector::DEFAULT_DEPLOY[:env] ||= {}
SingularityConnector::DEFAULT_DEPLOY[:env]['MESOS_HOST'] = "#{Resolv.getaddress('slave1.<%= @fqdn %>')}:5051"